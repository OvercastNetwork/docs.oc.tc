.container
    .jumbotron.subhead
        %h1 Modules
        %p.lead Game Settings
    %section
        .row
            = partial('modules_sidebar')
            .col-lg-9
                .page-header
                    %h1 Custom Rules
                :markdown
                    You can use the `<rules>` element to add custom rules to your map that are not already included in the standard Overcast Network rules. [Regions](/modules/regions) should be used to enforce access / block rules.

                        <rules>
                            <rule>A custom map rule</rule>
                        </rules>
